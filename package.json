{
  "name": "eggs",
  "version": "0.0.0",
  "description": "An isomorphic html data binding module, part of the mise.js suite.",
  "repository": {
    "type": "git",
    "url": "https://github.com/misejs/eggs.git"
  },
  "main": "lib/eggs.js",
  "scripts": {
    "test": "mocha --recursive --reporter spec --bail --check-leaks test/lib/",
    "test-travis": "mocha --recursive --reporter spec --bail --check-leaks test/lib/"
  },
  "keywords": [
    "data",
    "binding",
    "mise.js"
  ],
  "author": "Jesse Ditson <jesse.ditson@gmail.com>",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "mocha": "^2.0.1"
  }
}
